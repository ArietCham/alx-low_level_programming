#include "main.h"

/**
 * _sqrt_recursion - evaluate function sqrt
 * @n: integer
 * @x: integer
 * Return: evaluate sqrt
 */
int _sqrt_recursion(int n)
{
if (n == 1 || n == 0)
return (0);

else if (n * n < x)
return (_sqrt(n + 1, x));

else if (n * n == x)
return (n);

return (-1);

return (-1);
}
